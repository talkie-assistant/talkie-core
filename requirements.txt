# Dependencies for rifai_scholar_downloader (also compatible with project Pipfile)
# Use: pip install -r requirements.txt (in venv) or pipenv install -r requirements.txt
requests>=2.28.0,<3
beautifulsoup4>=4.11.0,<5
scholarly>=1.7.0
